import {
  DialogContent,
  Dialog,
  DialogTrigger,
  DialogTitle,
} from '../../../ui/dialog';

export const LoginForm = () => {
  return (
    <DialogContent aria-describedby={undefined}>
      <DialogTitle>Login</DialogTitle>
    </DialogContent>
  );
};
